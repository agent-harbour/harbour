#!/bin/zsh

set -euo pipefail

source "${0:A:h}/_common.sh"

require_var COLIMA_PROFILE
require_var WORKSPACE_ROOT

if colima_status; then
  cd "${WORKSPACE_ROOT}"
  exec colima stop -p "${COLIMA_PROFILE}"
fi

printf "Colima profile %s is already stopped.\n" "${COLIMA_PROFILE}"
